
{
  "name": "chatbot-server",
  "version": "1.0.0",
  "description": "Server for chatbot embedding",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Server-side build complete'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
